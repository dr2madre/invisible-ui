// The component ships as a subpath export (`@design-system/svelte/Sidebar.svelte`),
// like every styled component here: the barrel carries types, not markup.
export type { SidebarItem, SidebarSection } from "./types";
