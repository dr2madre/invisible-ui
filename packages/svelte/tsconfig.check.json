{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "types": ["svelte", "vite/client", "vitest/globals", "@testing-library/jest-dom/vitest", "node"]
  },
  "include": ["src", "test-types"],
  "exclude": [],
  "//": "Sources and tests alike: svelte-check resolves the .svelte module exports the tests import, which tsc cannot. test-types holds the test layer's declarations, outside what the package ships."
}
