{
  "component": "toggle-group",
  "selected": false,
  "props": [
    {
      "name": "variant",
      "type": "\"separate\" | \"segmented\"",
      "default": "\"separate\"",
      "required": false,
      "description": "Visual style. `separate` keeps each toggle's own style; `segmented` joins them."
    },
    {
      "name": "orientation",
      "type": "\"horizontal\" | \"vertical\"",
      "default": "\"horizontal\"",
      "required": false,
      "description": "Layout axis (purely visual — the group has no keyboard navigation of its own)."
    },
    {
      "name": "label",
      "type": "string | undefined",
      "default": "undefined",
      "required": false,
      "description": "Optional container name for screen readers (the group's aria-label); names the container, not the items."
    }
  ]
}
