{
  "component": "dialog",
  "selected": false,
  "props": [
    {
      "name": "triggerVariant",
      "type": "\"default\" | \"primary\" | \"secondary\" | \"ghost\" | \"danger\"",
      "default": "\"default\"",
      "required": false,
      "description": "Dialog — a styled modal window (WAI-ARIA dialog pattern). Behaviour and accessibility (open/close, `role=\"dialog\"` + `aria-modal`, labelling via the title, Escape to close) come from the headless dialog (`@design-system/core`); this adapter adds the modal DOM concerns: a portal to `<body>`, a focus trap, body scroll lock, a backdrop that closes on press, and focus management (focus moves into the panel on open and returns to the trigger on close).  Slots: `trigger` (the trigger button's content) and the default slot (the body). Pass an accessible `title` (required) and optional `description`. Colors, radius and elevation are themeable via `--ds-dialog-*`. / import { createDialog } from \"./create-dialog\"; import { portal } from \"../internal/portal\"; import Button from \"../button/Button.svelte\";  /** Visual variant for the trigger Button."
    },
    {
      "name": "open",
      "type": "boolean",
      "default": "false",
      "required": false,
      "description": "Initial open state."
    },
    {
      "name": "title",
      "type": "string",
      "default": null,
      "required": true,
      "description": "Accessible title naming the dialog."
    },
    {
      "name": "hideTitle",
      "type": "boolean",
      "default": "false",
      "required": false,
      "description": "Visually hide the title while keeping it as the dialog's accessible name (announced by screen readers via `aria-labelledby`). The title text is always required; this only controls whether it is shown."
    },
    {
      "name": "description",
      "type": "string | undefined",
      "default": "undefined",
      "required": false,
      "description": "Optional description, wired via `aria-describedby`."
    },
    {
      "name": "closeLabel",
      "type": "string | undefined",
      "default": "undefined",
      "required": false,
      "description": "Accessible label for the close button."
    },
    {
      "name": "footerClose",
      "type": "boolean",
      "default": "false",
      "required": false,
      "description": "Show an optional close/cancel button on the footer's leading (left) edge. The trailing (right) edge holds the action buttons from the `footer` slot."
    },
    {
      "name": "initialFocus",
      "type": "string | undefined",
      "default": "undefined",
      "required": false,
      "description": "CSS selector (within the panel) for the element to focus on open. When omitted, focus lands on the panel itself — never on the close button."
    },
    {
      "name": "onOpenChange",
      "type": "((open: boolean) => void) | undefined",
      "default": "undefined",
      "required": false,
      "description": "Called when the dialog opens/closes."
    }
  ]
}
