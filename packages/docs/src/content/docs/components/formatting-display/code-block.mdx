---
title: "Code Block"
---

import ImportSnippet from "../../../../components/ImportSnippet.astro";
import Preview from "../../../../components/Preview.astro";
import PropsTable from "../../../../components/PropsTable.astro";
import CodeBlockDemo from "../../../../demos/CodeBlockDemo.svelte";

<Preview>
  <CodeBlockDemo client:visible />
</Preview>

A block `<pre><code>` with an optional language caption and a copy button.

### Import

<ImportSnippet component="code-block" />

In Elements:

```html
<ds-code-block language="bash" code="pnpm install"></ds-code-block>
```

Set `code` as a property for a multi-line sample. It is always inserted as
text.

### Props

<PropsTable component="code-block" />

### Accessibility

- The copy button is labelled and announces success; the code is selectable.

### Example

```svelte
<CodeBlock code={"const x = 1;"} language="ts" />
```

Themeable via `--ds-code-block-*`.
