---
title: "Code"
---

import ImportSnippet from "../../../../components/ImportSnippet.astro";
import Preview from "../../../../components/Preview.astro";
import CodeDemo from "../../../../demos/CodeDemo.svelte";

<Preview>
  <CodeDemo client:visible />
</Preview>

Inline `<code>` for short snippets, identifiers and commands within prose.

### Import

<ImportSnippet component="code" />

In Elements:

```html
<p>Run <ds-code>pnpm install</ds-code> to get started.</p>
```

### Props

None — the content is the default slot. For multi-line blocks with a copy
button, use [CodeBlock](/invisible-ui/components/formatting-display/code-block/).

### Example

```svelte
<p>Run <Code>pnpm build</Code> to compile.</p>
```

Themeable via `--ds-code-*`.
