---
title: "Blockquote"
---

import ImportSnippet from "../../../../components/ImportSnippet.astro";
import Preview from "../../../../components/Preview.astro";
import PropsTable from "../../../../components/PropsTable.astro";
import BlockquoteDemo from "../../../../demos/BlockquoteDemo.svelte";

<Preview>
  <BlockquoteDemo client:visible />
</Preview>

A `<blockquote>` for quoted prose, with optional attribution.

### Import

<ImportSnippet component="blockquote" />

### Props

<PropsTable component="blockquote" />

The quote is the default slot.

### Example

```svelte
<Blockquote cite="Grace Hopper">We've always done it this way.</Blockquote>
```

Themeable via `--ds-blockquote-*`.
