---
title: "Keyboard shortcut"
---

import ImportSnippet from "../../../../components/ImportSnippet.astro";
import Preview from "../../../../components/Preview.astro";
import PropsTable from "../../../../components/PropsTable.astro";
import KbdDemo from "../../../../demos/KbdDemo.svelte";

<Preview>
  <KbdDemo client:visible />
</Preview>

A keyboard-shortcut hint, rendered with the semantic `<kbd>` element.

### Import

<ImportSnippet component="kbd" />

### Props

<PropsTable component="kbd" />

Pass a single key as the default slot, or a chord via the `keys` array.

### Example

```svelte
<Kbd>Esc</Kbd>
<Kbd keys={["⌘", "K"]} />
```

Themeable via `--ds-kbd-*`.
