---
title: "Tokens"
---

import Preview from "../../../components/Preview.astro";
import PaletteDemo from "../../../demos/PaletteDemo.svelte";

How the theme layer is put together and what to change first. The colors of the
presentation layer are shown below by function; the complete list of every
shipped token, with its value in both themes and what it points at, is the
[token catalog](/invisible-ui/presentation/token-catalog/).

<Preview>
  <PaletteDemo client:only="svelte" />
</Preview>

## Three tiers

Theming ships as `tokens.css` — **primitives** (the raw palette) feed
**semantic** tokens (roles) that the components consume — plus a third tier of
per-component knobs that live in the components themselves (see
[Component tokens](/invisible-ui/presentation/component-tokens/)):

```js
import "@design-system/svelte/tokens.css";
```

**Tier 1 — primitives.** Three groups, listed in the order you most likely
customize them: **brand** (`--ds-brand-*`), **feedback** (`--ds-feedback-*`) and
**neutrals** (`--ds-neutral-*`). The brand and feedback base tokens reference a
raw named scale (`--ds-purple-500`, `--ds-red-600`, …) taken from the
[color palette](/invisible-ui/presentation/color-palette/) — never a raw value.

The **primary** is Dioxazine Purple and doubles as the **selection / focus**
color: selected controls and the focus ring derive from it. There is no distinct
secondary *color* — `--ds-color-secondary` resolves to the same purple.
"Secondary" names a **function** (a present-but-not-prominent button), realized
by inverting the primary, not a second hue.

**Tier 2 — semantic (`--ds-color-*`).** Role tokens that reference the
primitives and **remap between light/dark**. Components only ever read these:

```css
--ds-color-primary: var(--ds-brand-primary);
--ds-color-danger: var(--ds-feedback-danger);
--ds-color-text: var(--ds-neutral-900);            /* light; → --ds-neutral-50 in dark */
--ds-color-info-surface: color-mix(in srgb, var(--ds-feedback-info) 8%, var(--ds-neutral-0));
```

> **Naming rule — decoupled from style.** A semantic token names a **role**
> (`surface`, `border`, `text`, `primary`, `emphasis-surface`…) or a **state**
> (`hover`, `pressed`, `disabled`, `focus-ring`…) — **never an appearance**.
> There is no `muted`, `soft` or `subtle`: the neutral control fill is
> `--ds-color-surface`, the hover overlay is `--ds-state-hover`. See ADR 0002
> (tokens decoupled from style) in the repository's `docs/adr/`.

## Surface, text and state roles

Beyond brand and feedback, the semantic layer covers the everyday neutrals —
each named for its role or state, and each remapping per theme: the page
(`--ds-color-background`), an area on it (`--ds-color-surface` and its hover),
text (`--ds-color-text`, `--ds-color-text-secondary`), the disabled state
(`--ds-color-disabled`, `--ds-color-text-disabled`), the interaction overlays
(`--ds-state-hover`, `--ds-state-pressed`) and focus
(`--ds-color-focus-ring`). Each one's role, value per theme and alias chain is
listed in the [token catalog](/invisible-ui/presentation/token-catalog/#tk-semantic).

**Two boundaries.** `--ds-color-border` is the quiet line between areas:
dividers, table rules, the edge of a panel. `--ds-color-control-border` is the
boundary that identifies an interactive control — fields, outlined buttons,
check controls, the switch track — and it reaches 3:1 against the page and
against a surface in both themes (WCAG 2.2 SC 1.4.11). Style a divider with the
first and a control with the second; giving dividers the stronger colour makes
the page shout, and giving controls the quieter one makes them fail the
criterion.

**Action fills.** The secondary button paints `--ds-color-secondary-surface`
with `--ds-color-on-secondary-surface` as its label; the danger button paints
`--ds-color-destructive-surface` with `--ds-color-on-destructive-surface`.
"Danger" names the feedback status, "destructive" names the action, so the
status tint under a message (`--ds-color-danger-surface`) and the fill of a
destructive button stay two different tokens. The earlier `-soft` names remain
as deprecated aliases that resolve to these roles and will be removed before
1.0 — the catalog marks each one with its replacement.

**Text emphasis (guideline).** Reach emphasis levels through the role tokens,
never by picking a neutral weight directly: `--ds-color-text` for what is being
read, `--ds-color-text-secondary` for what supports it, and
`--ds-color-text-disabled` only on a control that cannot be used.

## Shape & elevation (non-color)

Radius and shadow are tokens too, named for the role they shape rather than a
pixel size:

| Token | Role |
| --- | --- |
| `--ds-radius-control` | buttons, inputs, the close button |
| `--ds-radius-surface` | alerts, notices, cards |
| `--ds-radius-pill` | fully rounded: toggles, chips |
| `--ds-elevation-overlay` | the floating overlay shadow, per theme |

The current value of each is in the token catalog, under
[Radius](/invisible-ui/presentation/token-catalog/#tk-radius) and
[Elevation](/invisible-ui/presentation/token-catalog/#tk-elevation).

Components read these through their per-component override, e.g.
`var(--ds-button-radius, var(--ds-radius-control, …))` — set the role token to
restyle a whole class of shapes at once, or the component token for a one-off.
Every such knob is listed in
[Component tokens](/invisible-ui/presentation/component-tokens/).

## Customizing — in order of effort

1. **Brand** — set `--ds-brand-primary` (primary buttons, selection and the
   focus ring follow).
2. **Feedback** — set `--ds-feedback-*`; the status icons and the
   InlineNotification/Notification surfaces re-derive.
3. **Neutrals** — set the `--ds-neutral-*` ramp to restyle every UI surface,
   border and text color.

**Theme switch.** Follows `prefers-color-scheme` by default; force it per subtree
with `[data-theme="dark"]` / `[data-theme="light"]`.

**Accessibility.** The defaults are checked against WCAG 2 AA (text 4.5:1, icon
glyphs 3:1) by an automated test over `tokens.css`, and the
[token catalog](/invisible-ui/presentation/token-catalog/#tk-contrast) measures
and publishes each pair it lists, target by target — including the ones that do
not reach their target in this alpha. If you change
the brand or feedback hues, re-verify contrast: white text sits on the
brand/danger fills and on the status icon boxes, while the light Burnt Orange
warning uses a **dark** `--ds-color-on-warning`.
