---
"@design-system/elements": minor
---

Add `<ds-calendar>`, `<ds-date-picker>`, `<ds-date-range-picker>` and `<ds-time-field>`, ported from the Svelte adapter with the same classes, roles and stylesheets. Month and weekday names, date formats, the first day of the week and the hour cycle follow the element's locale, and every label comes from the message catalog.
