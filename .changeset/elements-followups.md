---
"@design-system/elements": patch
---

A disabled `<ds-dropdown-menu>` now looks disabled. `<ds-login-form>` names
the purpose of its fields (`autocomplete="username"` and `current-password`),
so browsers and password managers fill them. On the `<ds-sidebar>` rail each
destination shows its name as a tooltip on hover and focus.
