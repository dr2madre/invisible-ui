---
"@design-system/elements": patch
---

Render one SVG class attribute for Elements checkbox glyphs so the checked and
indeterminate symbols follow the native checkbox state instead of appearing at
the same time.
