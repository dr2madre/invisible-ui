---
"@design-system/elements": minor
"@design-system/svelte": patch
"@design-system/vue": patch
---

Add `ds-card`, `ds-tag`, and `ds-separator` with light-DOM composition,
reactive attributes, native semantics, and the existing adapter styles.

Record the shared Card, Tag, and Separator token coverage across the styled
adapters.
