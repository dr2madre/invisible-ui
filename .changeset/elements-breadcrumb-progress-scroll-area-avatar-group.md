---
"@design-system/elements": minor
---

Add `<ds-breadcrumb>`, `<ds-progress>`, `<ds-scroll-area>` and `<ds-avatar-group>`, ported from the Svelte adapter with the same classes, roles and stylesheets.
