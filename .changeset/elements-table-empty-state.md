---
"@design-system/elements": minor
---

Add an empty-state API to `ds-table` with the `empty-text` attribute and the
`renderEmpty` property. Empty content stays inside the native table and spans
all structural columns.
