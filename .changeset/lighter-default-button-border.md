---
"@design-system/svelte": patch
"@design-system/react": patch
"@design-system/vue": patch
"@design-system/elements": patch
---

Lighten the boundary of the default button. It is now the control boundary
mixed with the page behind it, so it reads less heavy while still standing out
from the page, and it follows the theme. The new `--ds-button-border` token
overrides it.
